{
  "config": {
    "step": {
      "user": {
        "title": "Connect to the matrix",
        "description": "The matrix does not report its own model, so choose it here. Another controller may already be connected — that is fine, the hardware accepts several at once.",
        "data": {
          "name": "Name",
          "host": "Host",
          "port": "Port",
          "model": "Model"
        },
        "data_description": {
          "host": "IP address or hostname of the matrix.",
          "port": "TCP control port. The default suits every model.",
          "model": "Determines how many inputs and outputs exist."
        }
      },
      "channels": {
        "title": "Choose the channels in use",
        "description": "Clear anything that is not wired up. Unused channels can be enabled later without losing the ones already set up.",
        "data": {
          "scan_interval": "Poll interval"
        },
        "data_description": {
          "scan_interval": "How often to re-read the matrix. It does not announce changes, so anything altered by another controller appears at the next poll."
        }
      }
    },
    "error": {
      "cannot_connect": "No Triad matrix answered. Check the address and that TCP port 52000 is reachable."
    },
    "abort": {
      "already_configured": "This matrix is already set up."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Channels and limits",
        "description": "Enable the channels in use, cap any zone that should not be driven to full, and set how often the matrix is polled.",
        "data": {
          "scan_interval": "Poll interval",
          "track_turn_on_volume": "Remember each zone's volume"
        },
        "data_description": {
          "scan_interval": "Lower values notice another controller's changes sooner, at the cost of more traffic.",
          "track_turn_on_volume": "Store a zone's volume as its turn-on volume once it settles, so it comes back where you left it. This is what Control4 does. Turn it off to set each zone's turn-on volume yourself, which replaces the read-only value with one you can edit."
        }
      }
    }
  },
  "issues": {
    "audio_sense_disabled": {
      "title": "Audio sense is switched off on {matrix}",
      "fix_flow": {
        "step": {
          "confirm": {
            "title": "Turn on audio sense",
            "description": "Audio-sense entities are enabled for this matrix, but it is not measuring, so they will stay unavailable.\n\nThe device reports the same value for \"no audio\" and \"not measuring\", so this cannot be shown as simply off without asserting something the hardware has not determined.\n\nSubmit to turn measuring on. If you did not intend to use audio sense, disable those entities instead and this will clear on its own."
          }
        },
        "abort": {
          "entry_unavailable": "That matrix is no longer set up, so there is nothing to change.",
          "cannot_enable": "The matrix did not accept the command. Check that it is reachable and try again."
        }
      }
    }
  },
  "services": {
    "set_eq_band": {
      "name": "Set EQ band",
      "description": "Set a band's frequency, gain and Q together. Omitted values are left alone, and the band is re-read once at the end rather than after each parameter.",
      "fields": {
        "frequency": {
          "name": "Frequency",
          "description": "Centre frequency in Hz. Snapped to the nearest band the matrix offers."
        },
        "gain": {
          "name": "Gain",
          "description": "Band gain in dB, -12 to +12."
        },
        "q": {
          "name": "Q",
          "description": "Band Q. The matrix offers 0.5, 0.6, 0.7, 0.8, 0.9, 1, 2 and 3."
        }
      }
    },
    "apply_eq_preset": {
      "name": "Apply EQ preset",
      "description": "Write all five EQ bands of a zone at once. Matches the presets the Control4 driver offers, so a zone sounds the same after the changeover. Define your own by making a script that calls this or Set EQ band.",
      "fields": {
        "preset": {
          "name": "Preset",
          "description": "Flat resets every band to 0 dB."
        }
      }
    },
    "send_raw": {
      "name": "Send raw command",
      "description": "Send one protocol command and return the reply. For reading something the integration does not model yet.",
      "fields": {
        "device_id": {
          "name": "Matrix",
          "description": "Which matrix to send to."
        },
        "command": {
          "name": "Command",
          "description": "Hex, starting FF 55. For example FF5504031EF500 reads output 1's volume."
        },
        "allow_write": {
          "name": "Allow writing",
          "description": "Required for any command without the F5 query marker. Those change the device, and it accepts out-of-range writes silently, so this is off by default."
        }
      }
    }
  }
}
